// PostCSS config override — intentionally empty.
// Tailwind CSS v4 is handled entirely by the @tailwindcss/vite plugin.
// This file prevents a Tailwind v3 postcss.config.js in a parent directory
// from being auto-discovered and conflicting with the v4 Vite plugin.
module.exports = {
  plugins: {},
};
