// PostCSS config — intentionally empty.
// Tailwind CSS v4 is handled entirely by the @tailwindcss/vite plugin.
// This file is explicitly .cjs (CommonJS) because package.json has "type": "module"
// Vite and PostCSS require the config file to be CommonJS for proper loading.
module.exports = {
  plugins: {},
};
